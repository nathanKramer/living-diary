# Telegram Bot (get from @BotFather)
TELEGRAM_BOT_TOKEN=

# Anthropic / Claude API key
ANTHROPIC_API_KEY=

# OpenAI API key (used for embeddings only)
OPENAI_API_KEY=

# Admin Telegram user ID — receives approval requests for new users, always pre-approved
# Send /start to the bot and check logs to find your user ID
ADMIN_TELEGRAM_ID=

# Seed Telegram user IDs (comma-separated, optional) — merged into persistent allowlist on startup
ALLOWED_USER_IDS=

# Model to use (default: claude-sonnet-4-5-20250929)
AI_MODEL=claude-sonnet-4-5-20250929

# Data directory for LanceDB and other persistent data
DATA_DIR=./data

# Timezone for scheduled messages (default: UTC)
TIMEZONE=UTC

#### WEB DASHBOARD

# Web dashboard port (default: 3000)
DASHBOARD_PORT=3000

# Web dashboard auth token (leave empty to disable auth for local-only use)
DASHBOARD_TOKEN=
